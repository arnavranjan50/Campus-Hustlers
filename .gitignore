# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

tsconfig.tsbuildinfo

# Ignore screenshots and generated images if user doesn't want them in git (or let's keep them if not specified, but let's exclude standard generated/screenshot files to keep repository clean, or maybe let's keep them? Let's check. Actually, let's keep it simple: ignore node_modules, dist, tsconfig.tsbuildinfo and standard logs/local envs).
.env
.env*.local
